#include "main.h"
/**
 * mul - multiplication of two integers
 * @i: first number to be multiplied
 * @j: second number to be multiplied
 * Return: the result of multiplication between i param and j param
 */

int mul(int i, int j)
{
	return (i * j);
}
